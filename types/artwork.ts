export type Category = { id: string; label: string };
export type OwnershipOption = { id: string; label: string };
export type Artwork = {
  title: string;
  artist: string;
  price: string;
  image: string;
};
